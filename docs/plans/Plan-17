# Plan 17: Performance Optimization & SEO

## Goal
Optimize the site for performance and search engines.

## Tasks
- [x] Add dynamic metadata (title, description, OpenGraph images) to all pages.
- [x] Implement `sitemap.ts` and `robots.ts`.
- [x] Optimize images using `next/image` with proper sizing.
- [x] Audit performance using Lighthouse and fix issues (LCP, CLS).
- [x] Ensure all interactive elements are accessible (ARIA labels).
