# Plan 16: Newsletter & Contact Forms

## Goal
Implement the contact form and newsletter signup.

## Tasks
- [x] Create a `ContactForm` component:
    - [x] Use **ShadCN Form** (with `useForm` and Zod).
    - [x] Use **ShadCN Input**, **Textarea**, **Button**.
    - [x] Use **Sonner** for success/error messages.
- [x] Create a `NewsletterForm` component (simple email input + button).
- [x] Implement Server Actions to handle form submissions.
- [x] Add a "Booking" specific form on the contact page.
